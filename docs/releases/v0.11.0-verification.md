# QuickPaste v0.11.0 验证报告

验证日期：2026-07-20

## 自动化门禁

- `npm ci`：通过，严格按 lockfile 安装依赖。
- `npm run check`：通过。
  - 治理测试 38 项通过；版本、仓库卫生、隐私、运行时边界与第三方声明检查通过。
  - 前端 31 个测试文件、664 项测试通过；TypeScript 类型检查与 Vite 生产构建通过。
  - Rust 格式检查、Clippy `-D warnings` 与 289 项单元测试通过。
- 中文 OCR 的语言选择、CJK 字间空格清理和 schema v10 重排队迁移均有自动化回归测试。

## 实际界面与 OCR 检查

- 在标准快速窗口和管理窗口检查了来源/时间轨道、工具栏、OCR 状态及操作按钮对齐。
- 在浅色与深色主题检查了图片详情；标准窗口恢复全宽主图，640×440 紧凑窗口同时保留图片和 OCR 摘要，没有再缩成细条。
- 使用本次问题所附中文截图直接调用 Windows OCR 适配层，结果重新识别出“正在记录”“OCR 文字”“全部”“文本”“代码”“链接”“图片”“返回历史”“剪贴板图片”等中文内容；临时探针未纳入仓库，也未保存用户图片。
- README 的快速面板与设置页面截图已从本次实际页面重新生成，并检查无私人路径或剪贴板内容。

## Windows 候选产物

| 产物 | 大小 | 版本 | SHA-256 |
| --- | ---: | --- | --- |
| `QuickPaste_0.11.0_x64-setup.exe` | 4,850,886 字节 | 0.11.0 | `1C784850EBE6717CF6331EB7DCBA7E010AA6822BD465F5905D5E823086016428` |
| `QuickPaste_0.11.0_x64-portable.zip` | 6,789,722 字节 | 0.11.0 | `2E1E6327247A23A76F8604C87B6EE3AD3809A1FC5880C1C4D8C4B83F2F871A30` |

- bundle 中只有一个 NSIS 安装包，没有 MSI、MSIX 或 APPX。
- 安装包和绿色版中的 `QuickPaste.exe` 均为 Windows x64 release 构建；可执行文件产品版本为 0.11.0。
- 当前产物没有 Authenticode 签名；GitHub 下载后需再次核对资产大小与 SHA-256。

## Pending real-machine

以下场景尚未完成固定样本量的真实机长循环，不能表述为已经通过：

- Windows 10/11 覆盖安装、卸载与重装。
- 100%–250% DPI、多显示器负坐标和四个任务栏边界。
- 中文图片批量重识别、长时间剪贴板监听、管理员目标回贴和 WebView2 缺失路径。

维护者明确选择将 v0.11.0 作为标准 GitHub Release，而不是 Pre-release；上述验证状态仍保持 `pending real-machine`。
