# QuickPaste v0.18.0 验证报告

验证日期：2026-07-22

本报告记录 v0.18.0 发布候选的本地自动化、安全审计、界面检查和产物验证证据。按维护者要求，本次不使用 Computer Use；真实应用完整交互矩阵、真实设备长时间运行、完整安装生命周期和完整 DPI 矩阵继续单独列为 `pending real-machine`。

## 规划范围

- 完成高优先级 A11Y-01、A11Y-02、DOC-01、CI-01、UX-01、UX-02 与 UX-03。
- 未执行需要大规模拆分 `App.vue`、Rust 模块或 IPC 类型体系的中优先级重构，避免把本次体验修复扩展为架构迁移。
- GOV-01 涉及维护者的许可证法律选择，本次未擅自决定。
- 本版本继承 v0.17.1 的复杂剪贴板格式包与粘贴前校验修复；Windows Chrome 风格多段 HTML 回归连续运行 20 次，失败 0 次。

## 自动化与安全审计

- `npm ci`：通过，共安装 140 个锁定包。
- 治理、版本、仓库链接、隐私、运行时边界和第三方许可检查：通过，46 项治理测试通过。
- 前端测试：34 个测试文件、721 项测试通过；TypeScript 检查与 Vite 生产构建通过。
- Rust 格式、Clippy（`-D warnings`）及测试：317 项测试通过。
- `npm audit --omit=dev --audit-level=high`（官方 npm registry）：通过，0 个漏洞。
- `cargo audit --file src-tauri/Cargo.lock`：退出码为 0，没有阻断性漏洞；报告 17 条允许的维护状态或 soundness 警告，来自 Tauri 的 GTK3/UNIC 间接依赖。QuickPaste 当前只交付 Windows，这些依赖不进入本次 Windows 可执行文件；警告仍由每周 RustSec 工作流持续监控。
- `npm run build:windows`：通过，只生成 v0.18.0 x64 NSIS 安装包，未生成 MSI、MSIX 或 APPX。
- GitHub Actions [CI 运行 #29886652194](https://github.com/zkwi/QuickPaste/actions/runs/29886652194)：发布时为 `in_progress`，作为推送后的异步复核，不替代或阻塞本地发布门禁。

## 界面检查

- 使用 Playwright CLI 在 800×580 浅色窗口检查标准模式：组合框与搜索帮助语义正确，标准行高为 44px，控制台 0 个错误。
- 使用 Playwright CLI 在 640×440 深色窗口检查紧凑模式：行高为 40px，完整显示 7 条记录（门槛为 6 条），来源建议列表与活动选项关系正确，控制台 0 个错误。
- 管理页检查到普通 `list` / `listitem` 语义、原生复选框及可独立访问的行操作。
- 悬浮预览自动化覆盖 400ms 延迟、仅截断文本触发，以及滚动、失焦、按键和指针操作取消。

## 发布产物

| 产物 | 字节 | SHA-256 |
| --- | ---: | --- |
| `quickpaste.exe` | 18,551,296 | `A6620EB7BE2FA0A26833492ACDCC4390EAB706208004E3C69EB8D99F4A6722B9` |
| `QuickPaste_0.18.0_x64-setup.exe` | 4,988,646 | `242532A4B4FCFE73A3D0407353CAA2A5678C2D8C972DF8DC2298227A0D164688` |
| `QuickPaste_0.18.0_x64-portable.zip` | 6,976,294 | `55D09B80ECFEA37CCCA7BAD45AE3EB03853207AF51A236CDBC161950A8EA05C1` |

最终构建核验结果：

- 绿色版只包含 `QuickPaste.exe` 和四份第三方许可文件，其中的可执行文件与 release 构建目录摘要一致。
- bundle 目录只包含一个 x64 NSIS 安装包，不包含 MSI、MSIX 或 APPX。
- 主程序 PE machine 为 `0x8664`，主程序和安装包产品版本均为 0.18.0，当前未进行 Authenticode 签名。
- 两个资产上传到草稿 Release 后已通过认证 API 重新下载；文件名、大小、SHA-256 与 GitHub `asset.digest` 均与本地候选一致。核验完成后已按 `draft=false`、`prerelease=false` 公开 [Release v0.18.0](https://github.com/zkwi/QuickPaste/releases/tag/v0.18.0)（Release ID `357750579`），并确认它是 Latest Release。

## Pending real-machine

- Windows 10/11 覆盖安装、卸载、保留 `data` 与重装。
- Chrome/WebView2、Office、记事本及远程剪贴板工具组合下的长期竞争与完整富文本兼容矩阵。
- 100%–250% DPI、多显示器负坐标、不同任务栏位置和辅助技术组合。
- 500 次暖唤起、10,000 次外部普通目标粘贴、100,000 次独立捕获与长时间监听。
- 管理员目标、UAC 取消、WebView2 缺失和在线 Bootstrapper 路径。
